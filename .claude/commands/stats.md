---
description: Show the gamified progress dashboard — rank, XP, phases, achievements, streak
allowed-tools: Bash(cargo run --bin stats), Read
---

Show my Rust mastery progress and motivate the next step.

1. Run `cargo run --bin stats` and show its output (the dashboard reads
   `progress.json`).
2. Read the next unchecked 🔥 high-leverage item from `ROADMAP.md`.
3. In one or two lines: call out the nearest milestone (next rank, or a phase
   about to complete) and name the specific next ladder to run to get there,
   e.g. "42 XP from ⚙️ Trait Smith — knock out `Rc`/`Arc` next."

Keep it punchy and motivating. Don't run any other cargo command.
