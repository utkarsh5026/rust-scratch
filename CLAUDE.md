# rust-scratch

A personal Rust **learn-by-doing** workspace. This is not a product — it's where
I drill Rust concepts until I actually understand them. The whole project is
built around one workflow: pick a concept, get a ladder of problems, and
implement each one myself with coaching (never handed the solution).

The engine for this is the **`rust-practice` skill** (`.claude/skills/rust-practice/`).

## The core loop

I name a concept → the skill builds a **ladder of 7-9 problems** (easy → mastery)
in a single file `src/bin/<concept>.rs` → I implement one rung at a time, running
`cargo run --bin <concept>` to check → I get staged hints if stuck, never the
answer up front.

Just say e.g. *"practice `Rc`/`RefCell`"*, *"drill iterators"*, or
`/rust-practice <concept>` to start a new ladder.

The full curriculum lives in **`ROADMAP.md`** — a 9-phase path from basics to
advanced Rust (ownership → traits → async internals → unsafe → perf → macros →
capstones). Each item there is a ladder to run with the practice skill. When I
ask "what's next" or "give me the next concept", pull the next unchecked item
from ROADMAP.md, build its ladder, and tick the box when I finish it.

### How a concept file is laid out

- One file per concept: `src/bin/<concept>.rs`.
- Each problem = a function I implement + a `check_N()` that asserts it.
- `main` runs `check_1(); check_2(); …` in order, so it replays every solved rung
  and stops at the first unfinished one (its `todo!` panics).
- A "Ladder:" comment at the top lists all rungs and marks which are done.
- The ladder goes all the way to mastery: foundations → mechanics → footguns &
  edge cases → real-world patterns → a build-it-from-scratch capstone.

## Completed concepts

| Concept | File | Run | Rungs |
|---------|------|-----|-------|
| `Cow` (Clone-on-Write) | `src/bin/cow.rs` | `cargo run --bin cow` | 9 — basics → serde zero-copy → reimplement from scratch |
| `Box` & the heap | `src/bin/box_heap.rs` | `cargo run --bin box_heap` | 9 — basics → recursive types → `dyn Trait`/`dyn Error`/`Box::leak` → hand-rolled linked list w/ iterative Drop |
| `Rc` / `Arc` | `src/bin/rc_arc.rs` | `cargo run --bin rc_arc` | 9 — shared ownership → `strong_count`/diamond DAG → `Rc<str>`/`make_mut` → cycle leak & `Weak` fix → `Arc<Mutex>` across threads → hand-rolled `MyRc<T>` |
| `Cell` / `RefCell` | `src/bin/cell_refcell.rs` | `cargo run --bin cell_refcell` | 9 — interior mutability → Cell/RefCell toolboxes → runtime borrow panic & re-entrancy → `Rc<RefCell>` graph → `Ref::map` → hand-rolled `MyRefCell` (`UnsafeCell` + flag + RAII guards) |
| Conversion traits | `src/bin/conversions.rs` | `cargo run --bin conversions` | 9 — `From`/`.into()` free → `impl Into` bounds → `From` powers `?` → `TryFrom` → orphan rule & reflexivity → `as` truncation vs `TryInto` → `AsRef<str>`/`AsRef<Path>`/`AsMut` → mini `serde_json::Value` (From in, TryFrom out, AsRef lookup) |
| Lifetimes in depth | `src/bin/lifetimes_depth.rs` | `cargo run --bin lifetimes_depth` | 9 — `longest` annotation → 3 elision rules → lifetimes in structs → `impl<'a>` & the &self elision gotcha → dangling/owned-vs-borrowed return → `'a: 'b` outlives bounds (variance seed) → lifetimes + generics + `'static` (`&'static` vs `T: 'static`) → borrowing `Iterator` (Item lifetime) → hand-rolled `StrSplit` (two lifetimes, zero-copy) |
| `Borrow` / `ToOwned` | `src/bin/borrow_toowned.rs` | `cargo run --bin borrow_toowned` | 9 — `ToOwned` as generalized Clone → `Borrow` views → `HashMap::get(&str)` & `K: Borrow<Q>` → `T::Owned` assoc type → Borrow-vs-AsRef contract (`CiString`) → needless-alloc footgun → Into-in/Borrow-out API split → closing the `Cow` loop → hand-rolled `MyBorrow`+`MyToOwned`+`MyCow` |
| `Drop` & ordering | `src/bin/drop_ordering.rs` | `cargo run --bin drop_ordering` | 9 — Drop at scope end → locals LIFO vs struct-fields declaration order → `mem::drop` early & `E0040` → drop flags (conditional move, no double-drop) → `mem::forget`/`replace`/`take` → RAII scope guard w/ `.cancel()` → `ManuallyDrop` custom field order (Miri-clean) → rollback-on-drop `Transaction` (commit disarms; panic still rolls back) |
| `Rc<RefCell<T>>` patterns | `src/bin/rc_refcell.rs` | `cargo run --bin rc_refcell` | 10 — shared-cell aha → owner structs share one cell → counts/ptr_eq/&Rc-vs-clone → double `borrow_mut` panic → borrow-across-call reentrancy → strong cycle leak (Drop never runs) → `Weak` fix + parent-pointer tree → observer/subject fan-out → hand-rolled doubly-linked list (`Rc` next / `Weak` prev) → iterative `Drop` (no stack overflow on long chains) |
| HRTB — `for<'a>` | `src/bin/hrtb.rs` | `cargo run --bin hrtb` | 9 — implicit `for<'a>` in `Fn(&T)` → spell it explicit → caller-picks vs callee-picks (return a borrow of a local) → HRTB on your own `Slicer<'a>` trait → "impl of Fn not general enough" (let-bound closure gets one lifetime; fn ptr/item are `for<'a>`) → named-lifetime trap (one `<'a>` fixed by caller) → `DecodeOwned: for<'de> Decode<'de>` (serde pattern; borrowers excluded) → `Box<dyn for<'a> Fn>` keeps trait object lifetime-free → parser-combinator capstone (`tag`/`number`/`map`/`then`) |
| Generic bounds & `where` clauses | `src/bin/generic_bounds.rs` | `cargo run --bin generic_bounds` | 9 — single bound (`PartialOrd+Copy`) → multiple bounds rewritten as `where` → bound the method not the struct (`Stack<NotDebug>`) → conditional method (`cmp_display`, E0599) → `?Sized` relaxes the implicit `Sized` (accept `str`) → where-only bounds (`I::Item: Display` projection; `for<'a> &'a C: IntoIterator` HRTB) → blanket impl + coherence (E0119) → conditional trait impl (`PartialEq`/`Clone for MyBox<T>`, what derive emits) → `IterExt` extension trait capstone (supertrait + blanket impl + per-method `where Self::Item:` bounds) |
| Associated types vs generic params | `src/bin/assoc_vs_generic.rs` | `cargo run --bin assoc_vs_generic` | 9 — same trait `type Item` vs `<T>` → one-impl-per-type vs many (E0119) → `Iterator<Item=u64>` equality bound + `I::Item` projection → hand-rolled `Iterator` for `Countdown` → generic `.into()` ambiguity (E0283) vs determined assoc output → `dyn Iterator<Item=..>` must pin assoc (E0191) → `Add<Rhs=Self>{type Output}` uses both → design a `Graph` trait's split → capstone: `MyIterator` + generic `Map` adapter threading the associated `Item` |
| Blanket impls & coherence | `src/bin/blanket_coherence.rs` | `cargo run --bin blanket_coherence` | 9 — unconditional `impl<T> Trait for T` → conditional `impl<T: Display>` → reconstruct `From`→`Into` blanket (`.into()` needs target type) → extension trait over `Iterator` (itertools pattern) → orphan rule E0117 (≥1 of {trait,type} local) → overlap E0119 (blanket vs concrete, no stable specialization) → uncovered-param E0210 (local-as-Self order; `Wrapper<T>` covers `T`) → newtype workaround + `Deref` ergonomics → capstone: sealed extension trait (private `Sealed` blanket gates `StatsExt`) |
| Static vs dynamic dispatch | `src/bin/dispatch.rs` | `cargo run --bin dispatch` | 9 — `<T>` vs `&dyn` (monomorphization vs vtable) → `impl Trait` arg position (generic sugar) vs return position (one hidden type) → `type_name::<T>()` proves a copy stamped per type → return-branch needs `Box<dyn>` (`impl Trait` can't) → `Vec<T>` can't mix but `Vec<Box<dyn>>` can → `-> Self` ok under generics, forbidden behind `dyn` (object safety) → closures: generic `F: Fn` vs `Vec<Box<dyn Fn>>` registry → enum dispatch (closed set, inline, no vtable, no alloc) → capstone: same pipeline static/dynamic/enum, three strategies one result |
| Error handling architecture | `src/bin/error_arch.rs` | `cargo run --bin error_arch` | 9 — `?`+`Box<dyn Error>` quick app error → hand-rolled enum (Display+Error+From+source) → `thiserror` derive (`#[error]`/`#[from]`) → `anyhow` context/`bail!`/`anyhow!` → source-chain walk + `downcast_ref` → E0277 `?`-won't-convert + `String`-error anti-pattern → thiserror-lib/anyhow-app boundary (typed error survives under context) → classification `is_retryable`+`#[non_exhaustive]` driving a generic retry loop → capstone: mini-anyhow (blanket `From`, `WrapErr::context`, `ContextError` source chain) |
| Custom error types | `src/bin/custom_errors.rs` | `cargo run --bin custom_errors` | 9 — by hand, no derives: Display+Error contract (empty `impl Error` unlocks `Box<dyn Error>`) → error enum w/ per-variant data → `source()` cause chain (keep cause OUT of Display) → `From`+`?` auto-convert (what `#[from]` generates) → `Box<dyn Error+Send+Sync>` bounds (`Rc`→`Arc` to cross threads) → downcast `is::<T>()`/chain-walk to root cause → `Backtrace::force_capture` (inherent getter; `Error::backtrace` unstable) → layered `AppError`→`ConfigError`→`ParseIntError` + anyhow `{:#}` chain printer → capstone: hand-built `Chain` iterator + `Report` `Caused by:` reporter |
| Newtype & zero-cost wrappers | `src/bin/newtype.rs` | `cargo run --bin newtype` | 9 — distinct identity (`Meters` vs `Seconds`, swap = type error) → derives forward to inner (`UserId` Debug/Copy/Ord) → `impl Add` defines the algebra (`m+s` won't compile) → `Deref` for ergonomics (wrap `String`, coerce to `str`) → the Deref leak footgun (curated API beats `Deref<Target=Vec>` for invariant-holding `SortedVec`) → orphan-rule escape hatch (`impl Display` for foreign type via newtype) → `#[repr(transparent)]` zero-cost proof (`size_of` match + sound slice reinterpret) → parse-don't-validate (`Email` private field + smart constructor) → capstone: phantom-typed `Id<T>` (`Id<User>` ≠ `Id<Post>`, `PhantomData`, hand-written impls so bound lands on `u64` not `T`) |
| Builder pattern | `src/bin/builder.rs` | `cargo run --bin builder` | 8 — consuming `self`→`Self` chain → `&mut self` builder (reusable, but clones) → `Option` fields + defaults (`unwrap_or_else` avoids eager alloc) → fallible `build() -> Result` (one validation checkpoint) → the `&mut` temporary-drop trap (E0716) + owning-binding fix → repeatable setters (`.route`/`.header` accumulate) + `impl Into<String>` args → typestate builder (`<Yes,No>` markers; `build()` only on `<Yes,Yes>` → missing field is a COMPILE error) → capstone: real `ServerConfig::builder()` (consume + Into + optionals + repeatable + validated build, moves not clones) |
| The typestate pattern | `src/bin/typestate.rs` | `cargo run --bin typestate` | 9 — `Door<State>` ZST markers + `PhantomData` → state-gated methods (`open()` on `Closed` only; wrong call won't compile) → consuming `self` transitions thread a payload through (stale handle moved away) → zero-cost `size_of` proof + sealed `State` trait (private supertrait) → the `PhantomData` footgun (E0392 "param never used") → runtime boundary: erase to an enum, re-enter typestate via `match` → typestate builder (`<U,M>` track required fields; `build()` only on `<Yes,Yes>`) → `impl<S: State>` + associated `const NAME` for behavior common to every state → capstone: TCP-like protocol (sealed states, typed transitions, generic accessors, `AnyConn::step` runtime event loop) |
| Threads & scoped threads | `src/bin/threads.rs` | `cargo run --bin threads` | 9 — `spawn`+`JoinHandle::join` → spawn-all-then-join keeps N threads parallel & ordered → `move` sends owned data (`'static`) → panicking thread caught in `join() -> Err`, downcast `Box<dyn Any>` payload (unwind-only) → the `'static` wall: borrowing a local in `spawn` fails (E0373/E0597, `move` only "fixes" by taking ownership) → `thread::scope` relaxes `'static` to outlives-scope, multiple shared `&` reads → `split_at_mut` → disjoint `&mut` chunks mutated in parallel (no Mutex) → map-reduce fan-in: chunk → partial sums → fold → capstone: generic `parallel_map<T,R,F>` (`T: Sync`, `R: Send`, `F: Fn(&T)->R + Sync`, `&f` reborrow, order-preserving `flat_map` — rayon-lite) |
| `Send` & `Sync` deeply | `src/bin/send_sync.rs` | `cargo run --bin send_sync` | 9 — `spawn` requires `Send` (move owned data) → `Sync` = shareable `&T` across scoped threads (`T: Sync ⟺ &T: Send`) → auto-derivation is structural + `assert_send`/`assert_sync` probes → predict-then-verify the std library (positives compile, negatives witnessed) → why `Rc` is `!Send`: reproduce the non-atomic-RMW lost-update race `Arc`'s `fetch_add` avoids → the four quadrants (`Cell`/`RefCell` = Send+!Sync; `MutexGuard` = !Send+Sync) → `Arc<Mutex<T>>` (Send+Sync) vs `Rc<RefCell<T>>` (neither) across `spawn` → escape hatch: `PhantomData<*const ()>` opt-out + `unsafe impl Send` opt-in (raw-ptr `Buffer`, SAFETY contracts) → capstone: hand-rolled `SpinLock<T>` (`UnsafeCell`+`AtomicBool`+RAII guard, `compare_exchange` Acquire/Release, `unsafe impl<T: Send> Sync` — only `T: Send` because mutual exclusion hands `T` between threads, Miri-clean) |
| API evolution & semver | `src/bin/semver.rs` | `cargo run --bin semver` | 9 — `required_bump` canonical rules → all-pub struct literal break (E0063/E0027) & private-field fix → `#[non_exhaustive]` struct (read fields, construct via a door) → enum variants (real E0004 via std `ErrorKind`; non_exhaustive forces `_`) → trait evolution (required-vs-defaulted method E0046; blanket-impl coherence) → sealed trait as semver tool (private `Sealed` supertrait ⇒ free to evolve) → auto-trait leakage (`Rc` field drops Send/Sync; `const _` compile-time guard) → generic bounds (loosen=safe, tighten=break; defaulted type params) → capstone: full `ApiChange→Bump` engine + a future-proofed `mod lib` whose v1.1 additions stay MINOR |
| Collections deep-dive | `src/bin/collections.rs` | `cargo run --bin collections` | 9 — `HashMap` word-count (`get`→`Option<&V>`) → `BTreeMap` sorted iter + `range(lo..=hi)` queries → `Entry` API kills double-lookup (`or_insert_with`/`and_modify`) → `HashSet` set algebra + dedup-in-order (`insert`→bool) → `VecDeque` ring buffer (O(1) both ends, BFS frontier) → `Borrow` lookup (`get("k")` no alloc) + key-mutation hazard via `Cell` (entry stranded in wrong bucket) → custom `Hash`/`Eq` & the `k==k' ⇒ hash==hash'` law (break it → silent entry loss) → choosing one decision matrix + FNV-1a custom hasher (`Hasher`+`BuildHasherDefault`, vs SipHash `RandomState`) → capstone: open-addressing `MyHashMap` (linear probing, tombstone reuse, load-factor resize/rehash) |
| Iterators end-to-end | `src/bin/iterators.rs` | `cargo run --bin iterators` | 9 — `filter`/`map`/`sum` chain → `iter`/`iter_mut`/`into_iter` (`&T`/`&mut T`/`T`) → adapter zoo (`enumerate`/`zip` stops-at-shorter/`flat_map`/`filter_map`/`fold`) → laziness proven (closure runs 0×; infinite `0..` tamed by `take`) → footguns: `into_iter` move trap (E0382), turbofish for `collect`, `collect::<Result<Vec<_>,_>>()` short-circuit → `impl Iterator` for `Fib` (one `next()` via `mem::replace`, all adapters free) → `IntoIterator` (the `for`-loop desugar) + `DoubleEndedIterator` `rev()` + `size_hint` → custom lazy `.pairs()` adapter via blanket extension trait (itertools pattern) → capstone: hand-built `MyIterator` engine (own trait + lazy `MyMap`/`MyFilter`/`MyTake` adapters + `collect_vec` consumer, pull-chain laziness from scratch) |
| Strings & text | `src/bin/strings_text.rs` | `cargo run --bin strings_text` | 9 — `str` view vs owned `String` (`&str` arg takes both via deref) → UTF-8 invariant: `len()` is bytes, `bytes`/`chars`/`char_indices` → slicing footgun (panic mid-codepoint; `get`/`is_char_boundary`; `len_utf8`-safe `behead`) → zero-copy parsing (`lines`/`split_once`/`trim`/`parse`, keys borrow input) → `OsStr`/`OsString` (OS text isn't UTF-8; `to_str`→Option, `to_string_lossy`→Cow Borrowed/Owned) → `Path`/`PathBuf` (`join`/`extension`/`file_name`/`set_extension` over string concat) → `CStr`/`CString` (NUL-terminated FFI; `CString::new` rejects interior NUL; `as_bytes` vs `as_bytes_with_nul`; `from_bytes_until_nul`) → bytes↔text validation (`as_bytes`/`into_bytes` free; `from_utf8` Result vs `from_utf8_lossy` Cow) → capstone: hand-rolled `decode_utf8` (lead-byte length, 10xxxxxx continuations, `char::from_u32`, overlong/range-strict) + `Utf8Chars` iterator == `str::chars()` |
| `impl Trait` & RPIT | `src/bin/impl_trait.rs` | `cargo run --bin impl_trait` | 9 — APIT arg = caller picks (sugar for `<T>`) → RPIT return = callee picks ONE hidden unspellable type → APIT==generic but impl-arg kills turbofish (no named param) → return closures/adapter chains by value (no `Box`/vtable) → one-hidden-type rule: if/else two iterators won't compile (E0308), fix via `Box<dyn>`/Vec-unify/`Either` enum → edition-2024 RPIT auto-captures in-scope lifetimes + `+ use<>` precise-capture opt-out (E0597 over-capture) → `async fn` IS `-> impl Future<Output=T>` (state machine via RPIT; Send iff awaited state is Send) → RPITIT + `async fn` in traits (1.75; NOT dyn-compatible, E0038) → capstone: lazy combinator toolkit (`compose`/`naturals`/`keep`/RPITIT `Stage`, `Box<dyn Fn>` only where runtime branching forces erasure) |
| Closures & `Fn`/`FnMut`/`FnOnce` | `src/bin/closures.rs` | `cargo run --bin closures` | 9 — capture env by `&` → three capture modes (`&`/`&mut`/`move`) → `Fn ⊂ FnMut ⊂ FnOnce` via `apply_fn`/`apply_mut`/`apply_once` (strictest→loosest bound) → desugar a closure by hand (struct + `call(&self)`/`call_mut(&mut self)`; receiver type IS the trait) → footguns: `FnOnce` moves a capture out → callable once (E0382); `FnMut` needs a `mut` binding (E0596) → returning closures: `impl Fn` (one hidden type) vs `Box<dyn Fn>` for branchy returns ("if/else incompatible types") → fn pointers: fn items & non-capturing closures coerce to `fn`, capturing ones can't (E0308); `Vec<fn(..)>` inline vs `Box<dyn>` → stdlib (`map`/`filter`/`sort_by_key` are FnMut) + closure factory (`make_validator -> impl Fn`) → capstone: `Dispatcher` event registry (`Vec<Box<dyn FnMut(&Event)>>`, `subscribe<F: FnMut + 'static>` boxes & erases, `emit(&mut self)` fans out; FnMut handlers keep internal state) |
| Modules & visibility | `src/bin/modules.rs` | `cargo run --bin modules` | 9 — module tree & paths (`crate::`/`super::`/`self::`) → `pub` opens a door (privacy is tree-relative; helper stays private) → field privacy (`pub struct` ≠ pub fields; private field + smart constructor guards an invariant, E0451/E0616) → `use` (local alias) vs `pub use` (re-export flattens a deep path) → leaking a private type through a `pub fn` (E0603/E0616 at the use site; `private_interfaces` in libs) → restricted visibility `pub(crate)`/`pub(super)`/`pub(in path)` (pick the narrowest reach) → facade pattern (`mod internal` private, curate surface via `pub use`; guts sealed) → sealed trait = private-module supertrait gates downstream `impl` → capstone: `inventory` mini-library (private `util`/`model`/`store` submodules, `pub(crate)` shared helper, invariant-guarding `Sku`, curated `pub use` front door) |
| `Mutex` / `RwLock` | `src/bin/mutex_rwlock.rs` | `cargo run --bin mutex_rwlock` | 9 — `Mutex<T>` guards DATA not code; `lock()`→RAII `MutexGuard` (`&mut T`, unlocks on drop; `let mut guard` for `DerefMut`) → `Arc<Mutex>` shared counter across N threads (clone-per-thread; lock held across read-modify-write or lost updates) → guard lifetime: snapshot + `drop(guard)` to shrink the critical section before slow work → `RwLock` many readers XOR one writer (`read()`=`&T` shared, `write()`=`&mut T` exclusive; writer-starvation caveat) → poisoning: panic-while-locked taints the lock, `lock()`→`Err`, recover via `unwrap_or_else(\|e\| e.into_inner())` (parking_lot/tokio don't poison) → non-reentrancy: std `Mutex` isn't recursive, locking twice in one thread self-deadlocks (proven with `try_lock`→`WouldBlock`) → lock-ordering ABBA deadlock: two locks taken in opposite orders cycle; fix = canonical order (lock lower `id`/addr first, bind guards to roles per branch) → `Mutex`+`Condvar` bounded blocking queue (`cv.wait(guard)` atomically unlocks+parks; **wait in `while` not `if`**; notify_all after mutate) → capstone: concurrent `Bank` (`Vec<Mutex<i64>>` fine-grained; deadlock-free `transfer` via lower-index-first + poison-tolerant `lock_recover`; 8×50k thread storm + watchdog; money conserved) |
| Channels | `src/bin/channels.rs` | `cargo run --bin channels` | 9 — `mpsc::channel` send-from-thread/recv → multi-producer via `tx.clone()` (the "m") → Receiver-as-iterator (`for v in rx` ends on disconnect; must `drop(tx)`) → `sync_channel(k)` bounded + `sync_channel(0)` rendezvous backpressure → the hang: stray Sender ⇒ recv blocks forever; `RecvError` (all senders gone) vs `SendError(v)` (receiver gone, value handed back) → non-blocking `try_recv` (split `Empty` keep-polling vs `Disconnected` stop) → worker pool: `Arc<Mutex<Receiver>>` shared job queue + results channel, lock only across recv → crossbeam: `Receiver: Clone` mpmc (no Mutex) + `select!` merge two streams, stop selecting a dead channel → capstone: hand-rolled `Channel<T>` (`Mutex<VecDeque>`+`Condvar`, blocking `recv` waits in a loop, sender-count disconnect via `notify_all` on last `Drop`, drain-before-Disconnect ordering) |
| Data parallelism with `rayon` | `src/bin/rayon_parallel.rs` | `cargo run --bin rayon_parallel` | 9 — `.iter()`→`.par_iter().sum()` first contact → adapter zoo (`map`/`filter`/`collect` preserves INPUT order, unlike `for_each`) → `reduce(id, op)` needs an identity CLOSURE (seeds each chunk) + fold-then-reduce (per-thread local acc → cross-thread combine) → `rayon::join(a,b)` the fork-join primitive (potentially-parallel, work-stealing) hand-rolled as recursive sum → break-even sweep: par LOSES on cheap/memory-bound work (~hundreds-µs overhead floor), WINS only when work/cores ≫ overhead → non-associative reduce is a silent bug (subtraction ⇒ 200 distinct answers/200 runs; split shape is unspecified) → shared-state wall (`par_iter().for_each(\|x\| vec.push(..))` won't compile — closure is `Fn`+`Sync`, needs `&mut`; fix = `map().collect()` lock-free & ordered, or `Mutex<Vec>` works-but-worse) → real APIs: `par_sort_unstable` + `par_bridge` (adapt any sequential `Iterator: Send`, serial-pull bottleneck) → capstone: hand-rolled `parallel_map<T:Sync,R:Send,F:Fn(&T)->R+Sync>` (join+cutoff+concat, pass `&F` down) + parallel quicksort (Lomuto partition → `split_at_mut` disjoint halves → `rayon::join` both sides); plus the dead-code-elimination benchmark footgun (`black_box` unused results) |
| Cargo features & `cfg` | `src/bin/features_cfg.rs` | `cargo run --bin features_cfg` | 9 — `cfg!()` macro (runtime bool, both arms compile) → `#[cfg(...)]` attribute (item exists or not; twin defs, would-be `E0428`) → first feature flag (`[features]` + `#[cfg(feature)]`, `--features`) → `cfg_attr` (conditional `derive`, the serde idiom) → **additivity law** (mutually-exclusive features collide under unification; redesign to accumulate) → missing-symbol trap (`cfg!` keeps both arms ⇒ `E0425`; gate the call site & imports with `#[cfg]`) → optional dependency as a feature (`optional = true` + `dep:rand`, off ⇒ not compiled at all) → feature graphs (`default`, umbrella `full = [...]` transitive enabling, `--no-default-features`) → capstone: mini config/output module (`cfg(target_os)` path + additive `json`/`pretty` renderers + `cfg_attr` serde derive + `cfg!` build report) |
| 🔥 Testing | `src/bin/testing.rs` + `practice/testing_lab/` | `cargo test --bin testing` ; `cargo test -p testing_lab` | 9 — first `#[cfg(test)] mod tests` + `use super::*` + `assert_eq!` (test module is free in release builds) → assertion toolbox (`assert!`/`assert_ne!`, custom messages, why `assert_eq!` beats `assert!(a==b)`) → `Result<(),E>`-returning tests so `?` works (vs `.unwrap()` panic) → `#[should_panic]` + tighten with `expected="…"` → execution control (`#[ignore]`+`--ignored`, name filter, `--nocapture` captures stdout, `--test-threads=1` & the parallel-shared-state footgun) → assertion footguns (bare `should_panic` passes on the WRONG panic; never `assert_eq!` raw floats — use epsilon; unit tests reach privates) → integration tests in `tests/` = separate crate, public API only (E0603 on private), shared helpers via `tests/common/mod.rs` not `common.rs` → doctests (runnable `///`, hidden `#` setup, `?` via trailing `# Ok::<(),E>(())`, `should_panic`/`compile_fail`/`no_run` fences) → capstone: `Ledger` lib in `testing_lab/` tested every way at once (white-box unit on private `entries`/invariant, black-box integration w/ custom `assert_balance`, doctest per public item incl. `should_panic` + `?`) |


## Project layout

```
rust-scratch/
├── Cargo.toml
├── CLAUDE.md
├── ROADMAP.md                  # the 9-phase mastery curriculum (what to practice next)
├── .claude/
│   ├── skills/rust-practice/   # the practice-ladder coach (the heart of this project)
│   └── commands/               # /experiment, /run, /explain, /list, /document
├── docs/                       # mdBook knowledge base -> GitHub Pages
│   ├── book.toml
│   └── src/
│       ├── SUMMARY.md          # nav (a page MUST be listed here to be built)
│       ├── intro.md
│       ├── concepts/<concept>.md   # one distilled note per finished ladder
│       └── meta/{adding-a-note,template}.md
└── src/
    ├── main.rs                 # scratch pad for quick throwaway code (cargo run)
    └── bin/                    # one file per concept ladder
```

## Knowledge base (mdBook -> GitHub Pages)

Finished ladders get a **distilled notes page** in `docs/src/concepts/`, published
as a searchable site at `https://utkarsh5026.github.io/rust-scratch/` via the
`Deploy docs` GitHub Action (`.github/workflows/docs.yml`). Local preview:
`mdbook serve docs --open`. See `docs/src/meta/adding-a-note.md` for the workflow
and `docs/src/meta/template.md` for the page format.

## Available dependencies

Preinstalled so ladders never need setup:

- **async**: `tokio` (full), `futures`, `async-trait`
- **errors**: `anyhow`, `thiserror`
- **serde**: `serde`, `serde_json`
- **http**: `reqwest`
- **utils**: `rand`, `tracing`, `tracing-subscriber`

Add more under `[dependencies]` in `Cargo.toml` as a concept needs them.

## Guidance for Claude

- **Default to the `rust-practice` skill.** When I name a Rust concept I want to
  learn/practice/drill, build a ladder — don't just explain it or write a demo.
- **Coach, don't solve.** Never pre-fill a solution body. Use `todo!()` / `// TODO`
  and give hints in stages; reveal the answer only if I explicitly ask.
- **One rung at a time.** Show the full ladder up front, but scaffold and hand
  over only the current problem.
- **Reach mastery.** Take ladders all the way (edge cases, real-world patterns,
  a from-scratch capstone). When in doubt, add another rung.
- **Keep the file compiling.** A scaffolded `todo!` should panic at runtime, not
  break compilation — otherwise earlier solved rungs stop running.
- **Don't rewrite my solved rungs.** They're notes-to-self. Append new rungs/files.
- When a concept ladder is finished: (1) add a row to the **Completed concepts**
  table here, (2) add a row to the **Completed concepts** table in `README.md`
  (terser, phase-grouped view), (3) tick its checkbox in `ROADMAP.md`, and
  (4) write its distilled notes page in `docs/src/concepts/`, register it in
  `docs/src/SUMMARY.md`, and tick its row in `docs/src/intro.md` (use
  `docs/src/meta/template.md`). The site auto-deploys on push to `master`.
- For pure deep teaching (not practice), defer to the `/rustacean` skill.
