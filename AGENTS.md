## Learned User Preferences
- When fixing Rust ladder errors, make the smallest scoped code change, verify with the matching `cargo run` or `cargo check`, and give a concise explanation.

## Learned Workspace Facts
- `progress.json` tracks completed Rust ladder rungs with date, phase, concept, rung, tier, hints, first_try, and miri_clean fields.
